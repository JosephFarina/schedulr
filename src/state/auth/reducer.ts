import { RAuth } from 'src/models'

export const initialState: RAuth = {}

const auth = (state = initialState, action) => {
  switch (action.type) {


    default:
      return state
  }
}

export default auth
