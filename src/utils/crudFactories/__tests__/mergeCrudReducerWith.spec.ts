import { mergeCrudReducerWith } from 'src/utils'


describe('crud: #mergeCrudReducerWith', () => {
  const initialState = {
    'hello': 'initial state'
  }

  it('should return provided initial state when undefined', () => {
    // const state = mergeCrudReducerWith(initialState)
  })

})
